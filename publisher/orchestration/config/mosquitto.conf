# Local development broker only. Bind loopback so it is not exposed on a LAN.
listener 1883 127.0.0.1
# Authentication is intentionally disabled for this local-only example.
# Enable authentication before exposing the broker beyond loopback.
allow_anonymous true
